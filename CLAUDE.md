# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

> **상태: 골격.** 이 디렉터리는 아직 비어 있고 스택도 정해지지 않았습니다.
> 아래 각 섹션은 코드가 생기는 대로 **실제로 확인한 내용만** 채웁니다.
> 확인 없이 추측으로 채우지 마세요 — 틀린 CLAUDE.md는 없는 것보다 나쁩니다.

## 상위 저장소와의 관계

이 디렉터리는 `hello/` 저장소 안에 있습니다. 저장소 루트에도 `CLAUDE.md` 가 있고,
루트 쪽은 **빌드 도구가 없는 정적 HTML 사이트**를 설명합니다.

- 루트 규칙(디자인 토큰, 3단 테마 계층, header/footer 수동 복제 등)은 루트의
  `index.html` / `about.html` / `assets/` 에 대한 것입니다.
  vibe-week1 이 다른 스택을 쓴다면 그 규칙을 그대로 가져오지 마세요.
- 반대로 vibe-week1 이 같은 정적 HTML 방식으로 간다면, 규칙을 여기에 복붙하지 말고
  루트 `CLAUDE.md` 를 참조하고 **차이점만** 이 파일에 적으세요.
- git 저장소는 루트 하나입니다. 여기서 `git init` 을 다시 하지 마세요.

## 개발 명령

<!-- TODO: 스택이 정해지면 채울 것.
     실제로 실행해서 동작을 확인한 명령만 적습니다. 최소한:
     - 개발 서버 실행 (포트 포함)
     - 빌드 (있다면)
     - 테스트 전체 실행 / 단일 테스트 실행
     - 린트 / 타입 체크
     없는 항목은 "없음" 이라고 명시하세요. 비워 두면 있는 줄 알고 찾게 됩니다. -->

아직 없음.

## 구조

<!-- TODO: 디렉터리 트리와 각 위치의 역할.
     `ls` 로 알 수 있는 내용은 생략하고, 파일을 열어 봐야만 알 수 있는
     "무엇이 어디에 사는가" 를 적으세요. -->

아직 없음.

## 아키텍처 메모

<!-- TODO: 여기가 이 파일의 핵심입니다.
     파일 하나만 읽어서는 안 보이는, 여러 파일에 걸친 구조를 적으세요:
     - 데이터/요청이 흐르는 경로
     - 한 곳을 고치면 같이 고쳐야 하는 곳 (루트의 header/footer 복제 같은 것)
     - 되돌리기 어렵거나 의도적으로 그렇게 한 설계 결정과 그 이유
     반대로 이런 건 적지 마세요: 일반적인 코딩 규칙, 파일 목록 나열,
     코드를 보면 바로 아는 사실. -->

아직 없음.

## 제약

<!-- TODO: 하지 말아야 할 것. 예:
     - 추가하면 안 되는 의존성이나 도구
     - 손대면 안 되는 생성 파일 / 벤더 디렉터리
     - 커밋하면 안 되는 것 -->

아직 없음.
